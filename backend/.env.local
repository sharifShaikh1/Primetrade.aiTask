# Server Configuration
# Default port changed to 4000 to avoid conflicts on some systems
PORT=4000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/intern

# JWT Configuration
JWT_SECRET=tyujwereqqdx
JWT_EXPIRE=7d

# Redis Configuration
REDIS_URL=redis://default:ks53eaKepcBaNXvEVr0euo0p6qHZ6vSX@redis-10549.c212.ap-south-1-1.ec2.redns.redis-cloud.com:10549
# If your Redis URL already contains credentials (e.g. redis://:password@host:port
# or redis://user:password@host:port), leave REDIS_PASSWORD blank. Otherwise
# set REDIS_PASSWORD to your Redis password (optional).
REDIS_PASSWORD=

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
